import express from "express";
const router = express.Router();

router.get("/", (req, res) => {
  /* Implement logic */
});
router.post("/", (req, res) => {
  /* Implement logic */
});

export default router;
